const express = require('express');
const router = express.Router();

// Commands are now sent via WebSocket, not HTTP
// This file is kept for backwards compatibility but routes are deprecated

module.exports = router;